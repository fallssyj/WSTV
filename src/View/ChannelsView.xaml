<UserControl
    x:Class="WSTV.View.ChannelsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WSTV.ViewModels"
    d:DataContext="{d:DesignInstance vm:ChannelsViewModel}"
    d:DesignHeight="800"
    d:DesignWidth="500"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Row 0: 标题行  -->
        <TextBlock
            Grid.Row="0"
            Margin="16,16,16,5"
            FontSize="28"
            Foreground="{StaticResource TextPrimaryBrush}"
            Text="{Binding SourceTitle}" />

        <!--  Row 1: 搜索框  -->
        <TextBox
            Grid.Row="1"
            Margin="16,5,16,8"
            Style="{StaticResource SearchTextBoxStyle}"
            Tag="搜索频道..."
            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

        <!--  Row 2: 分组芯片  -->
        <ListBox
            Grid.Row="2"
            Margin="16,0,16,8"
            ItemContainerStyle="{StaticResource GroupChipItemStyle}"
            ItemsSource="{Binding GroupTitles}"
            SelectedItem="{Binding SelectedGroup, Mode=TwoWay}"
            Style="{StaticResource GroupChipListBoxStyle}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Text="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--  Row 3: 频道网格  -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl
                x:Name="ChannelGrid"
                Margin="0,0,0,200"
                VerticalAlignment="Top"
                ItemsSource="{Binding Channels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource WidthToColumns}, ConverterParameter=200}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Margin="6,4"
                            Background="{StaticResource BackgroundSecondaryBrush}"
                            CornerRadius="10"
                            Cursor="Hand">
                            <!--  双击播放（功能留空，后续实现）  -->
                            <Border.InputBindings>
                                <MouseBinding
                                    Command="{Binding DataContext.PlayChannelCommand, ElementName=ChannelGrid}"
                                    CommandParameter="{Binding}"
                                    Gesture="LeftDoubleClick" />
                            </Border.InputBindings>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="85" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <!--  频道预览区  -->
                                <Border
                                    Grid.Row="0"
                                    Background="{StaticResource BackgroundTertiaryBrush}"
                                    CornerRadius="10,10,0,0">
                                    <Grid>
                                        <!--  频道 Logo（有 TvgLogo 时显示）  -->
                                        <Image
                                            MaxHeight="100"
                                            Margin="8"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Source="{Binding LogoImage}"
                                            Stretch="Uniform">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TvgLogo}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                        <!--  无 Logo 时显示 AirplayThin 图标  -->
                                        <Path
                                            Width="36"
                                            Height="36"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Data="{StaticResource Youtube}"
                                            Fill="{StaticResource TextSecondaryBrush}"
                                            Stretch="Uniform">
                                            <Path.Style>
                                                <Style TargetType="Path">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TvgLogo}" Value="">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Path.Style>
                                        </Path>
                                        <!--  分组标签  -->
                                        <Border
                                            Margin="0,0,6,6"
                                            Padding="5,2"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Background="#80000000"
                                            CornerRadius="4">
                                            <TextBlock
                                                FontSize="10"
                                                Foreground="{StaticResource TextSecondaryBrush}"
                                                Text="{Binding DisplayGroupTitle}" />
                                        </Border>
                                    </Grid>
                                </Border>
                                <!--  信息条  -->
                                <Grid Grid.Row="1" Margin="10,6,6,6">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="13"
                                        Foreground="{StaticResource TextPrimaryBrush}"
                                        Text="{Binding DisplayName}"
                                        TextTrimming="CharacterEllipsis" />
                                    <!--  收藏按鈕  -->
                                    <Button
                                        Width="28"
                                        Height="28"
                                        HorizontalAlignment="Right"
                                        Command="{Binding DataContext.ToggleFavoriteCommand, ElementName=ChannelGrid}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource CardIconButtonStyle}">
                                        <Path
                                            Width="16"
                                            Height="16"
                                            Stretch="Uniform">
                                            <Path.Style>
                                                <Style TargetType="Path">
                                                    <Setter Property="Data" Value="{StaticResource Heart}" />
                                                    <Setter Property="Fill" Value="{StaticResource TextSecondaryBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Favorite}" Value="True">
                                                            <Setter Property="Data" Value="{StaticResource HeartFill}" />
                                                            <Setter Property="Fill" Value="{StaticResource AccentPrimaryBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Path.Style>
                                        </Path>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
